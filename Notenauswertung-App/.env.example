# Optional: Basis-URL des Backends (ohne trailing slash).
# Leer lassen = relative URLs / Vite-Proxy (Standard bei npm run dev).
# Beispiel Desktop-Dev gegen laufendes Backend:
# VITE_API_BASE_URL=http://127.0.0.1:3000
