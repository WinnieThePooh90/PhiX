# Ports für docker compose (Variante Docker)
FRONTEND_PORT=1990
BACKEND_PORT=3000
# Host-Port für PostgreSQL (im Container immer 5432).
# Bei "port is already allocated": 5433 setzen ODER alten Container/ lokale PostgreSQL stoppen.
DB_PORT=5432
